import React, { Component, ReactNode } from 'react';

export class NotFoundComponent extends Component {
    render(): ReactNode {
        return (
            <div>
                404 not found
            </div>
        );
    }
}
