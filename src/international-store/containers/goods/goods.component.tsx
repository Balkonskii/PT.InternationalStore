import React, { Component, ReactNode } from 'react';

export class GoodsComponent extends Component {
    render(): ReactNode {
        return (
            <div>goods</div>
        );
    }
}
