import React, { Component, ReactNode } from 'react';

export class CartComponent extends Component {
    render(): ReactNode {
        return (
            <div>cart</div>
        );
    }
}
